libtend_dep = declare_dependency(link_with : libtend, include_directories : includedir)

executable('tendxx',
           'main.cpp',
           include_directories : includedir,
           dependencies : deps,
           link_with : libtend)
